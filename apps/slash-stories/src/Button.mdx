import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} name="Button" />

## Button

### Import

```tsx
import { Button } from "@axa-fr/canopee-react/distributeur";
```

### Use

#### Button Example

```tsx
const SubmitButton = () => (
  <Button type="submit" variant="secondary">
    My Submit button
  </Button>
);
```

#### Playground

<Canvas of={ButtonStories.Playground} />

<Controls of={ButtonStories.Playground} />

## Button variation

The button has 6 variants, `primary` (default variant), `secondary`, `validated`, `danger`, `ghost` and `ghost-reverse`.\
To choose them, simply use the variant `props`.

### Primary

The Primary CTA component allows you to create one and only one primary action within: a page, a horizontal step train page, a vertical step train section or a modal.

<Canvas of={ButtonStories.Primary} sourceState="shown" />

### Secondary

The CTA Secondary component allows you to create one or more secondary actions within projects.

<Canvas of={ButtonStories.Secondary} sourceState="shown" />

### Validated

<Canvas of={ButtonStories.Validated} sourceState="shown" />

### Danger

<Canvas of={ButtonStories.Danger} sourceState="shown" />

### Ghost

<Canvas of={ButtonStories.Ghost} sourceState="shown" />

### Ghost Reverse

<Canvas of={ButtonStories.GhostReverse} sourceState="shown" />

## Button Size

The button has 2 sizes, "default" and "small" configurable from the props size

<Canvas of={ButtonStories.Small} sourceState="shown" />

## Button With icons

It is possible to add an icon on the right or left via the `leftIcon` and `rightIcon` props.\
This props expects a react component, you can use an svg or a glyphicon component.

<Canvas of={ButtonStories.WithIconLeft} sourceState="shown" />

<Canvas of={ButtonStories.WithIconRight} sourceState="shown" />
