import { Canvas, Controls, Meta } from "@storybook/addon-docs/blocks";
import * as TitleStories from "./Title.stories";

<Meta of={TitleStories} />

## Title

> **Deprecated prop**: The `classModifier` prop is deprecated. Use `className` to add custom CSS modifier classes instead.

### Use of Title

```tsx
import { Title } from "@axa-fr/canopee-react/distributeur";

export const MyComponent = () => (
  <Title className="af-title--custom">Sample Title</Title>
);
```

The title can also contain any React component. Simply pass it into :

- the `contentLeft` prop if you want it to be displayed between the title and the separator.
- the `contentRight` prop if you want it to be displayed after the separator.

```tsx title="content.js"
import { Title } from "@axa-fr/canopee-react/distributeur";

export const MyComponent = () => (
  <Title
    className="af-title--custom"
    contentLeft={<Button>Left button</Button>}
    contentRight={<Button>Right button</Button>}
  >
    Title with content
  </Title>
);
```

## Playground

<Canvas of={TitleStories.Template}></Canvas>

<Controls of={TitleStories.Template} />
