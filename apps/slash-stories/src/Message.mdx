import { Canvas, Controls, Meta } from "@storybook/addon-docs/blocks";
import * as MessageStories from "./Message.stories.tsx";

<Meta of={MessageStories} />

## Message

### Import

```tsx title="App.tsx"
import { Message } from "@axa-fr/canopee-react/distributeur/";
```

### Use

```tsx title="App.tsx"
export const MessageComponent = () => (
  <Message variant="warning" title="Warning: information is missing" />
);
```

The property `closeButtonAriaLabel` allows projects to change the aria-label of the close button, making it more accessible.

### Variants

There are 4 variants available for the Message component, `error` is the default one:

<table style={{ width: "100%" }}>
  <thead>
    <tr>
      <th>Variant</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>warning</td>
    </tr>
    <tr>
      <td>error</td>
    </tr>
    <tr>
      <td>success</td>
    </tr>
  </tbody>
</table>

## Playground

<Canvas of={MessageStories.Default} />

<Controls of={MessageStories.Default} />
