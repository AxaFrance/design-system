import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as MessageStories from "./Message.stories";

<Meta of={MessageStories} name="Message" />

# Message

To use the message import it like that:

```tsx
import { Message } from "@axa-fr/design-system-apollo-react";

const MyComponent = () => (
  <>
    <Message>My message</Message>
    <Message variant="error">My error message</Message>
  </>
);
```

## Playground

<Canvas of={MessageStories.Playground}></Canvas>

<Controls of={MessageStories.Playground} />

## Variants

Use the `variant` prop to change the look of the message. [See the examples](#all-variants).

## All variants

<Canvas of={MessageStories.All} layout="fullscreen" />
