import { Canvas, Controls, Meta } from "@storybook/blocks";
import * as CardMessageStories from "./CardMessage.stories";

<Meta of={CardMessageStories} name="CardMessage" />

# CardMessage

### Import

```tsx
import { CardMessage } from "@axa-fr/design-system-apollo-react/lf";
```

### Use

```tsx
export const CardMessageComponent = () => (
  <CardMessage
    title="This is a title"
    text="I am the text of the card message"
    variant="info"
  />
);
```

## Playground

<Canvas of={CardMessageStories.Default} />

<Controls of={CardMessageStories.Default} />
