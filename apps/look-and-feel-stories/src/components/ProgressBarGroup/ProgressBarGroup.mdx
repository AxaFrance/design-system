import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as ProgressBarGroupStories from "./ProgressBarGroup.stories";

<Meta of={ProgressBarGroupStories} />

# ProgressBarGroup

A multi-step progress indicator for wizards, forms, or any process with several steps.

## Usage

```tsx
import { ProgressBarGroup } from "@axa-fr/canopee-react/client";

<ProgressBarGroup
  currentStep={1} // Index of the current step (0-based)
  currentStepProgress={50} // Progress of the current step (0-100)
  stepsCount={4} // Total number of steps
/>;
```

## Props

- `currentStep` (number): Index of the current step (0-based, required)
- `currentStepProgress` (number): Progress of the current step (0-100, default: 0)
- `stepsCount` (number): Total number of steps (required)
- `max` (number): Maximum value for progress (default: 100)
- `className` (string): Custom class for the group

## Example

<Canvas of={ProgressBarGroupStories.Playground} sourceState="shown" />

<Controls of={ProgressBarGroupStories.Playground} />
