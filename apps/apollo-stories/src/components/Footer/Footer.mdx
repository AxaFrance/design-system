import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as FooterStories from "./Footer.stories";

<Meta of={FooterStories} name="Footer" />

# Footer

To use the Footer import it like that:

```tsx
import { Footer } from "@axa-fr/canopee-react/prospect";

const MyComponent = () => (
  <Footer
    links={links}
    socialMedias={socialMedias}
    copyright={copyright}
    expandLinkText={expandLinkText}
  />
);
```

## Playground

<Canvas of={FooterStories.Playground} />

<Controls of={FooterStories.Playground} />
