import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as InputTextStories from "./InputText.stories";

<Meta of={InputTextStories} name="InputText" />

# InputText

To use the InputText import it like that:

```tsx
import { InputText } from "@axa-fr/design-system-apollo-react";

const MyComponent = () => (
  <>
    <InputText
      value="John Doe"
      label="Label"
      placeholder="Placeholder"
      helper="Informations complémentaires"
      name="name"
      id="nameid"
      required
      buttonLabel="my btn"
    />
  </>
);
```

## Placeholder

<Canvas of={InputTextStories.InputTextPlaygroundStory} />

<Controls of={InputTextStories.InputTextPlaygroundStory} />
