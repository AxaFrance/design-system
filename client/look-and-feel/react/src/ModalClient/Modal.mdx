import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as ModalStories from "./Modal.stories";

<Meta of={ModalStories} name="Modal Client" />

# Modal

To use the modal import it like that:

```tsx title="test.js"
import { Modal } from "@axa-fr/design-system-look-and-feel-react";

const MyComponent = () => <Modal isOpen={true} />;
```

## Playground

<Canvas of={ModalStories.Playground} />
<Controls of={ModalStories.Playground} />
