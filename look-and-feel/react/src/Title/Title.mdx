import { Controls, Canvas, Meta } from "@storybook/blocks";
import * as TitleStories from "./Title.stories";

<Meta of={TitleStories} />

## Title

### Use of Title

#### Title with icon

```tsx
import { Title } from "@axa-fr/design-system-look-and-feel-react";

export const TitleWithIcon = () => (
  <Title
    firstSubtitle="Sous-titre 1"
    secondSubtitle="Sous-titre 2"
    icon={<Svg src={article} />}
  >
    Titre de la page
  </Title>
);
```

<Canvas of={TitleStories.TitleWithIcon} />

<Controls of={TitleStories.TitleWithIcon} />

#### Title without icon

```tsx
import { Title } from "@axa-fr/design-system-look-and-feel-react";

export const TitleWithIcon = () => (
  <Title firstSubtitle="Sous-titre 1" secondSubtitle="Sous-titre 2">
    Titre de la page
  </Title>
);
```

<Canvas of={TitleStories.TitleWithoutIcon} />

<Controls of={TitleStories.TitleWithoutIcon} />
