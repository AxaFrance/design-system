import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as FooterStories from "./Footer.client.stories";

<Meta of={FooterStories} name="Footer Client" />

# Footer

To use the Footer import it like that:

```tsx title="test.js"
import { Footer } from "@axa-fr/design-system-react/client";

const MyComponent = () => (
  <Footer
    links={links}
    socialMedias={socialMedias}
    copyright={copyright}
    expandLinkText={expandLinkText}
  />
);
```

## Playground

<Canvas of={FooterStories.Playground} />

<Controls story="Footer" />
