import { Canvas, Controls, Meta } from "@storybook/addon-docs";

import * as HeaderStories from "./Header.stories";

<Meta of={HeaderStories} />

# Header

## Import

```tsx
import { Header } from "@axa-fr/design-system-react/agent";
```

## Use

```tsx
const HeaderComponent = (children: React.ReactNode) => (
  <Header>{children}</Header>
);

export default HeaderWithNameComponent;
```

## Stories

### Header With Name

<Canvas of={HeaderStories.HeaderWithName} />
<Controls of={HeaderStories.HeaderWithName} />

### Header With Name And Infos

<Canvas of={HeaderStories.HeaderWithNameAndInfos} />
<Controls of={HeaderStories.HeaderWithNameAndInfos} />
./Header.stories
