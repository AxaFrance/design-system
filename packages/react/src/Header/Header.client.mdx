import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as HeaderStories from "./Header.client.stories";

<Meta of={HeaderStories} name="Header Client" />

# Header

To use the header import it like that:

```tsx title="test.js"
import { Header } from "@axa-fr/design-system-react/client";

const MyComponent = () => <Header />;
```

## Playground

<Canvas of={HeaderStories.Playground} />

<Controls story="Header" />
