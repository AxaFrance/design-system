import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as MultiSelectStories from "./MultiSelect.stories";

<Meta of={MultiSelectStories} />

# MultiSelect

The MultiSelect component allows users to select multiple or one option(s) from a drop-down list.

<Canvas of={MultiSelectStories.MultiSelectStory} />
<Controls of={MultiSelectStories.MultiSelectStory} />

# MultiSelect with one value

<Canvas of={MultiSelectStories.MultiSelectOneValueStory} />
<Controls of={MultiSelectStories.MultiSelectOneValueStory} />

# MultiSelectInput

<Canvas of={MultiSelectStories.MultiSelectInputStory} />
<Controls of={MultiSelectStories.MultiSelectInputStory} />
