import { Canvas, Controls, Meta } from "@storybook/addon-docs";
import * as SliderStories from "./Slider.stories";
import * as SliderInputStories from "./SliderInput.stories";

<Meta of={SliderStories} />

# Slider

## Default Slider

To use the Slider, import it like that:

```tsx
import { Slider } from "@axa-fr/design-system-slash-react/slash";

const MyComponent = () => (
  <Slider
    id={id}
    name={name}
    options={options}
    onChangeComplete={({ id, name, value }) => {
      console.log(id, name, value);
    }}
  />
);
```

<Canvas of={SliderStories.SliderStory} />
<Controls of={SliderStories.SliderStory} />

## SliderInput

To use the SliderInput, import it like that:

```tsx
import { SliderInput } from "@axa-fr/design-system-slash-react/slash";

const MyComponent = () => (
  <SliderInput
    id={id}
    name={name}
    label={label}
    options={options}
    onChangeComplete={({ id, name, value }) => {
      console.log(id, name, value);
    }}
    message={message}
    messageType={MessageTypes.error}
  />
);
```

<Canvas of={SliderInputStories.SliderInputStory} />
<Controls of={SliderInputStories.SliderInputStory} />
