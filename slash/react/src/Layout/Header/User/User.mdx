import { Controls, Canvas, Meta } from "@storybook/addon-docs";
import * as UserStories from "./User.stories";

<Meta of={UserStories} />

# User

## Import

```tsx
import { User } from "@axa-fr/design-system-slash-react/slash";
```

## Use

```tsx
const UserComponent = () => (
  <User
    href="/#"
    name="Pierre Martin"
    profile="profile"
    title="Voir mon profil"
  >
    {children}
  </User>
);
export default UserDefault;
```

## Stories

### User With Link

<Canvas of={UserStories.UserWithLink} />
<Controls of={UserStories.UserWithLink} />

### User Without Link

<Canvas of={UserStories.UserWithoutLink} />
<Controls of={UserStories.UserWithoutLink} />
