mode: ContinuousDelivery
tag-prefix: 'slash/'
next-version: 1.0.0
commit-message-incrementing: Disabled
branches:
  release:
    tag: RC
    regex: ^releases?-slash[/-](?<BranchName>.+)
    commit-message-incrementing: Disabled
    source-branches:
    - main